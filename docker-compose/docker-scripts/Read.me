Build scripts should be run only AFTER maven packaging (at least run 'mvn clean package' in root dir).
Moreover, I'm too lazy to add "anywhere" run for scripts, so you need to cd in this dir and run .sh from here.